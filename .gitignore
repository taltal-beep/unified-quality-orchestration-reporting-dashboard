__pycache__/
*.pyc
.DS_Store
.venv/
venv/

# Local secrets (copy from .env.example)
.env

# Default local SQLite DB when DATABASE_URL is unset (engine/db_config.py)
uqo_history.db
testo_history.db

# Cursor IDE local state (debug logs, plans, rules, etc.)
.cursor/

# Local e2e test scratch (cleanup ledgers, etc.)
.artifacts/

# Built Python distribution artifacts
dist/
build/
*.egg-info/

# Local release-gate run logs
artifacts/release-gate-*/

# Mirrored report HTML for Streamlit static serving
static/allure_report.html
static/allure_report/
static/allure_reports/
static/locust_report.html
static/behave/

# Persisted run history (SQLite + per-run HTML snapshots)
artifacts/uqo_run_history.sqlite
artifacts/testo_sandbox.db
artifacts/history/

# ReportPortal local stack Postgres data
data/reportportal/postgres/

# Local/generated artifacts
.coverage
artifacts/allure-report/
artifacts/allure-results/
artifacts/allure-results-archive/

# Runtime logs
logs/

# Frontend workspace artifacts
frontend/node_modules/
frontend/dist/

# Allure Report 3 (repo root)
node_modules/
.testo/

