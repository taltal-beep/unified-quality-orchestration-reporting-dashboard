streamlit>=1.36.0
python-dotenv>=1.0.0
pluggy>=1.5.0
docker>=7.0.0

# Database (PostgreSQL + SQLModel)
sqlmodel>=0.0.24
psycopg[binary]>=3.2.0

# Explicit core stack (sandbox + runners)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
requests>=2.32.0
boto3>=1.34.0
pytest>=9.0.0
httpx>=0.27.0
# BehaveX 4.6+ imports behave.configuration (Behave 1.3+)
behave>=1.3.0,<2
locust>=2.43.0

# Orchestrator CLI (BehaveX mode; 4.6+ JSON formatter contract + parallel)
behavex>=4.6.0

# Allure + hooks
allure-pytest>=2.15.0
allure-behave>=2.15.0
allure-python-commons>=2.15.0
matplotlib>=3.10.0

# Phase 4: Grafana/Influx integration
influxdb-client>=1.48.0

# Orchestrator unit tests
pytest-cov>=5.0.0

# Phase 4.5: advanced sandbox auth + uploads
pyjwt>=2.9.0
python-multipart>=0.0.9
